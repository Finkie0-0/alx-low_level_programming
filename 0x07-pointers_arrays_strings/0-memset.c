#include "main.h"
/**
 * _memset - fills memory with a constant byte.
 * @s - memory area pointer
 * @b - constant byte set
 * @n - first bytes of memory
 * Return: dest
 */
char *_memset(char *s, char b, unsigned int n);
{
	unsigned int i;
	/*For statement to increment for a position*/
	for (i = 0, i < n, i++)
	{
		/*Moves up position by one*/
		*(s + i) = b
	}
	return (s);
}
